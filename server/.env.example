# Variáveis de ambiente para o backend
# Copie este arquivo para .env e configure os valores

# Segredo para gerar tokens JWT (mude para algo único)
JWT_SECRET=sua-chave-secreta-aqui-mude-isso

# Credenciais do administrador do Painel ADM
ADMIN_EMAIL=admin@meusite.com
ADMIN_PASSWORD=minhaSenhaForte123

# Porta do servidor (padrão: 3000)
PORT=3000
